#include<stdio.h>
#include<math.h>

int main()
{
    double a=10.4,b=10.6;
    //printf("floor is %d\n ceiling is %d\nround is %d\n",floor(10.4),ceil(10.5),round(10.6))
    int fl=sqrt(b);
    printf("\n%d",fl);
    int ans=floor(a);
    printf("\n%d",ans);
    return 0;
}
